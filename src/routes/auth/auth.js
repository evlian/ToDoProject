require('dotenv').config();
const bcrypt = require('bcryptjs');

function authorize_user(app, connection)
{
    // app.post("/login", async (req, res) => {
    //     try {

    //         console.log(hashedPassword);

    //     }
    // });
}